import { ArrowRight, BarChart3, BookOpen, CheckCircle2, Clock3, Code2, Play, Target, Timer, TrendingUp } from "lucide-react";
import { useEffect, useState } from "react";
import { Button, Card, Empty, Progress } from "../components/ui";
import { apiRequest, formatClock, formatSeconds } from "../lib/api";

const sampleActivities = [
  { name: "個人開発", color: "bg-indigo-500", seconds: 7800 },
  { name: "大学", color: "bg-amber-500", seconds: 6000 },
  { name: "英語", color: "bg-emerald-500", seconds: 2700 },
  { name: "読書", color: "bg-pink-500", seconds: 1800 },
];

export function DeveloperPreviewPage() {
  const [enabled, setEnabled] = useState<boolean | null>(null);
  useEffect(() => { apiRequest<{ enabled: boolean }>("/api/dev-preview").then((data) => setEnabled(data.enabled)).catch(() => setEnabled(false)); }, []);
  if (enabled === null) return <div className="grid min-h-screen place-items-center text-sm text-muted">プレビューを確認中…</div>;
  if (!enabled) return <div className="grid min-h-screen place-items-center bg-canvas px-4"><Card className="w-full max-w-md text-center"><p className="text-lg font-black">ページが見つかりません</p><p className="mt-2 text-sm leading-6 text-muted">このプレビューはテスト環境でのみ利用できます。</p></Card></div>;
  return <div className="min-h-screen bg-canvas"><header className="border-b border-line bg-white"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-4 sm:px-6"><div className="flex items-center gap-2 font-black"><span className="grid size-9 place-items-center rounded-xl bg-brand text-white"><Timer size={20} /></span>じかん帳</div><span className="rounded-full bg-amber-50 px-3 py-1.5 text-xs font-black text-amber-700">開発者プレビュー</span></div></header><main className="mx-auto max-w-6xl px-4 pb-12 pt-7 sm:px-6"><div className="mb-7 rounded-3xl bg-[linear-gradient(115deg,#635bff,#8b7dff)] p-6 text-white shadow-lg shadow-brand/20 sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-bold tracking-[.18em] text-indigo-100">TEST ENVIRONMENT</p><h1 className="text-3xl font-black tracking-tight sm:text-4xl">今日は何をしますか？</h1><p className="mt-2 text-sm text-indigo-100">ログインなしで、画面の雰囲気を確認できます。</p></div><div className="rounded-2xl bg-white/15 px-4 py-3 text-left backdrop-blur"><p className="text-xs text-indigo-100">現在記録中</p><p className="mt-1 text-lg font-black">個人開発</p><p className="font-mono text-2xl font-black">{formatClock(5075)}</p></div></div><div className="mt-6 flex flex-wrap gap-2">{sampleActivities.map((activity) => <button key={activity.name} className="rounded-xl bg-white/95 px-4 py-3 text-sm font-bold text-ink shadow-sm transition hover:bg-white">{activity.name}</button>)}</div><Button className="mt-3 w-full bg-white text-brand hover:bg-indigo-50 sm:w-auto"><Play size={16} className="mr-2 fill-current" />タイマーを始める</Button></div><div className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><Card><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-bold text-muted">今日の記録</p><p className="mt-1 text-4xl font-black text-brand">5時間05分</p></div><div className="grid size-12 place-items-center rounded-2xl bg-indigo-50 text-brand"><Clock3 size={23} /></div></div><div className="space-y-4">{sampleActivities.map((activity) => <div key={activity.name}><div className="mb-1.5 flex items-center justify-between text-sm"><span className="font-bold">{activity.name}</span><span className="font-bold">{formatSeconds(activity.seconds)}</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${activity.color}`} style={{ width: `${Math.round((activity.seconds / 7800) * 100)}%` }} /></div></div>)}</div></Card><Card><div className="mb-4 flex items-center gap-2"><TrendingUp size={18} className="text-brand" /><h2 className="font-black">最近の記録</h2></div><div className="divide-y divide-line">{[["09:00 - 10:20", "大学", "1時間20分"], ["11:00 - 13:10", "個人開発", "2時間10分"], ["15:30 - 16:15", "英語", "45分"]].map(([time, name, duration]) => <div key={time} className="flex items-center justify-between py-3 first:pt-0"><div><p className="text-xs text-muted">{time}</p><p className="mt-1 text-sm font-bold">{name}</p></div><span className="text-sm font-bold">{duration}</span></div>)}</div></Card></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Card><div className="mb-5 flex items-center gap-2"><BarChart3 size={18} className="text-brand" /><h2 className="font-black">振り返り</h2><span className="ml-auto rounded-lg bg-indigo-50 px-2.5 py-1 text-xs font-bold text-brand">今週</span></div><div className="flex h-36 items-end justify-around gap-3 px-2">{[["月", 48], ["火", 72], ["水", 38], ["木", 88], ["金", 60], ["土", 30], ["日", 15]].map(([day, height]) => <div key={String(day)} className="flex h-full flex-1 flex-col items-center justify-end gap-2"><div className="w-full max-w-8 rounded-t-lg bg-brand/80" style={{ height: `${height}%` }} /><span className="text-[11px] text-muted">{day}</span></div>)}</div><div className="mt-5 flex items-center justify-between border-t border-line pt-4"><span className="text-sm text-muted">今週の合計</span><span className="text-lg font-black text-brand">12時間35分</span></div></Card><Card><div className="mb-5 flex items-center gap-2"><Target size={18} className="text-brand" /><h2 className="font-black">今週の目標</h2><span className="ml-auto text-xs font-bold text-muted">2件</span></div><div className="space-y-5">{[["英語", "2時間40分 / 5時間", 53], ["個人開発", "7時間20分 / 10時間", 73]].map(([name, detail, progress]) => <div key={String(name)}><div className="mb-2 flex justify-between text-sm"><span className="font-bold">{name}</span><span className="text-muted">{detail}</span></div><Progress value={Number(progress)} /></div>)}</div><div className="mt-5 rounded-2xl bg-emerald-50 p-3 text-sm text-emerald-700"><CheckCircle2 size={16} className="mr-1 inline" />この調子で続けましょう。</div></Card></div><div className="mt-5 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-dashed border-amber-300 bg-amber-50 px-4 py-3 text-xs text-amber-800"><span><Code2 size={15} className="mr-1 inline" />サンプルデータで表示しています。実データは保存されません。</span><span className="font-bold">/preview</span></div></main></div>;
}
